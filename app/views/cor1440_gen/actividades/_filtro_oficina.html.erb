<% sel = [] %>
<% if params && params[:filtro] && 
    params[:filtro][:busoficina] %>
  <% sel = params[:filtro][:busoficina] %>
<% end %>
<%= f.input :busoficina,
  collection: Msip::Oficina.order('nombre'),
  include_hidden: false,
  input_html: { 
    class: "tom-select", 
    'data-action': 'change->enviarautomatico#enviar',
    multiple: true, 
  },
  label: false,
  label_method: :nombre,
  selected: sel,
  value_method: :id
%>
