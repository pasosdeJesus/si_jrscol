# frozen_string_literal: true

module Msip
  # A futuro parámetros empleados para crear vista materializada
  # para evitar volver a crear o a refrescar cuando no se requiere.
  class ParamsVistam < ActiveRecord::Base
  end
end
